require("toggleterm").setup({
    direction = 'vertical',
    open_mapping = [[<c-\>]],
    size = 90,
    shade_terminals = true,
    start_in_insert = true,
    persist_mode = false,
    close_on_exit = true,
    auto_scroll = true,
    hide_numbers = false,
}
)
